# PubCast AI — Base Dependencies
# Install: pip install -r requirements.txt
#
# For Architect (local GGUF inference), also run:
#   pip install -r requirements-architect.txt
#
# For cloud LLM providers (OpenAI, Gemini, Anthropic), uncomment the relevant lines below.

# ── Core framework ──────────────────────────────────────────────────────────────
fastapi>=0.109.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
starlette>=0.35.0,<1.0
pydantic>=2.0,<3.0
aiofiles>=23.2.1,<25.0

# ── HTTP client (Ollama, external APIs) ─────────────────────────────────────────
httpx>=0.27.0,<1.0

# ── Form/multipart parsing ──────────────────────────────────────────────────────
python-multipart>=0.0.7

# ── Database ────────────────────────────────────────────────────────────────────
aiosqlite>=0.19.0

# ── File type detection ─────────────────────────────────────────────────────────
# python-magic is NOT used. File type detection uses mimetypes from the stdlib,
# extension mapping, and a safe unknown fallback.

# ── Authentication helpers ──────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── Cloud LLM providers (install as needed) ─────────────────────────────────────
# openai>=1.12.0
# google-generativeai
# anthropic>=0.20.0

# ── Standalone Iteration Wallet (optional, independent of main app) ──────────────
# flask>=3.0
# flask-cors>=4.0
