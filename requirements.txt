# PubCast AI v5.0 — Base Dependencies
# Install: pip install -r requirements.txt
#
# For Architect (local GGUF inference), also run:
#   pip install -r requirements-architect.txt
#
# For cloud LLM providers (OpenAI, Gemini), uncomment the relevant lines below.

# ── Core framework ──────────────────────────────────────────────────────────────
fastapi>=0.109.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
starlette>=0.35.0,<1.0
pydantic>=2.0,<3.0

# ── HTTP client (Ollama, external APIs) ─────────────────────────────────────────
httpx>=0.27.0,<1.0

# ── Form/multipart parsing ──────────────────────────────────────────────────────
python-multipart>=0.0.7

# ── Database ────────────────────────────────────────────────────────────────────
aiosqlite>=0.19.0

# ── File type detection (stdlib fallback — no platform-specific binary needed) ──
# python-magic is NOT used. File type detection uses mimetypes (stdlib) + extension
# mapping with a safe unknown fallback. Zero external dependency.

# ── Cloud LLM providers (install as needed) ─────────────────────────────────────
# openai>=1.12.0          # Uncomment for OpenAI bots
# google-generativeai     # Uncomment for Gemini bots
# anthropic>=0.20.0       # Uncomment for Claude bots

# ── Standalone Iteration Wallet (optional, independent of main app) ──────────────
# flask>=3.0
# flask-cors>=4.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
